# 단계별 검토 체크리스트

PRD v1.1 기준. 각 단계는 아래 항목을 **모두 통과**해야 다음 단계로 진행한다.
자동 검증(빌드/타입/테스트)은 명령으로, 실시간·UI는 Firebase 에뮬레이터 + 헤드리스 테스트로 확인한다.

검증 토대(공통)
- `npm run build` 무에러
- `tsc --noEmit` 타입 에러 0
- 린트 통과
- 시크릿은 코드에 없음(.env로만 주입), `.env.example` 최신

---

## Step 1 — 폴더 구조 + 데이터 스키마  ✅ 승인 완료

- [x] 폴더 구조 합의(routes/features/theme/firebase/models/content/worker)
- [x] 타입 9종 정의(SessionMeta/State/Content, Group, Student, Artwork, Appreciation, AuctionItem, Bid, GroupResult)
- [x] 규칙 확정: 돈=원 정수, 단계 게이트=state.phase, 학생 키=번호, 발문/답변=가변 배열

---

## Step 2 — 스캐폴드 + Firebase 연결 + 세 라우트 빈 껍데기  ✅ 통과 (2026-06-26)

- [x] Vite + React + TS + Tailwind 프로젝트 빌드 성공 (`npm run build` ✓)
- [x] `tsc --noEmit` 통과 (에러 0)
- [x] `/teacher` `/play` `/tv` 라우트가 각각 빈 화면으로 렌더(HTTP 200, SPA 라우팅)
- [x] `src/theme/tokens.ts`에 §7.1 토큰 반영, Tailwind config가 이를 참조(CSS에 Cormorant·text-cream 컴파일 확인)
- [x] `src/firebase/app.ts`가 env에서 설정 주입 + 에뮬레이터 연결 분기(`VITE_USE_EMULATOR`)
- [x] `src/models/*`가 PRD §6 스키마와 1:1 일치
- [x] `src/firebase/paths.ts` 경로 헬퍼 + `hooks.ts` 구독 훅 골격
- [x] `.env.example` + README(로컬 실행/에뮬레이터/배포/Worker) 작성
- [~] Firebase 에뮬레이터 + dev 서버 동시 기동: dev 서버 기동 확인. 에뮬레이터 부팅은 Step 3 수직 슬라이스에서 실사용 검증.

---

## Step 3 — 수직 슬라이스(세션 생성→입장→단계 동기화)  ✅ 통과 (2026-06-27, 실제 RTDB)

검증: `node scripts/verify-slice.mjs` → 수신 시퀀스 `lobby → prologue → gallery`

- [x] 교사 콘솔: 세션 생성 → 6자리 코드 발급, RTDB에 `meta`/`state`/`content` 시드 기록
- [x] 학생 `/play`: 코드+번호+이름 입장 → `students/{번호}` 기록
- [x] 동일 번호 재입장 idempotent(같은 레코드 인계, 이름/uid 갱신)
- [x] 교사 단계 전환(lobby→prologue→gallery…) → 구독 클라이언트 실시간 반영
- [x] 탭 2~3개(교사/학생/TV) 동시 구독·동기화(헤드리스 멀티클라이언트 테스트 통과)
- [x] Firebase 익명 인증 동작
- [~] 학생 게이트/새로고침 복구: 동일 `useRtdbValue` 구독 경로로 구현됨(UI는 Step 4 진행 중 브라우저로 함께 확인)

실제 Firebase 프로젝트: `art-auction-8552` (Spark 무료, RTDB + 익명 인증)

---

## Step 4 — 기능별 (각 기능 통과 시 다음 기능)

### 4a. 감상 + 해설  ✅ 통과 (2026-06-27, 실제 RTDB)
검증: `node scripts/verify-appreciation.mjs`
- [x] 작품별 발문(가변 개수) 패널, 한 번에 하나씩 + 자유 텍스트 저장 → `appreciations` 기록
- [x] 제출 후 해설 화면 표시 → 확인 후 다음 작품 (GalleryView 해설 오버레이)
- [x] 진행 도트/화살표로 본 작품 재열람
- [x] 감상 수정 시 작성시점(submittedAt) 보존 + updatedAt 갱신
- [x] 교사 작품 관리(추가/목록/삭제), 배치=공통회랑/분기, 경매 대상 플래그
- [~] 현재 열린 단계만 수정 / 수행평가 표식: 데이터·구조 준비됨. 단계 게이트 강제는 후속 단계에서 UI 적용

### 4b. 회랑 분기  ✅ 통과 (2026-06-27)
- [x] 교사 문 개수 설정, 문별 작품 배치(ArtworkManager 분기 N)
- [x] 학생 문 선택 입장 → 문별 작품 감상 저장(BranchView → GalleryView 재사용)
- [x] 모둠원이 다른 문 선택 유도(안내 문구)

### 4c. 경매 + 타이머  ✅ 통과 (2026-06-27, 실제 RTDB)
검증: `node scripts/verify-auction.mjs`
- [x] 모둠 대표 1명만 입찰(대표 지정 + 대표 기기 게이트)
- [x] 최소 호가 단위 강제, 현재가+단위 이상만 유효(too-low 거부)
- [x] 모둠 예산 상한 초과 입찰 차단(budget 거부)
- [x] 작품 랜덤 순서, 교사가 올리기/다음 작품 진행
- [x] 교사 타이머: 호가 들어오면 자동 연장, 무호가 시 자동 낙찰(teacher 패널 자동 마감)
- [x] 유찰 처리 + 유찰작 재경매
- [x] **RTDB 트랜잭션으로 최고가 원자적 갱신**(동시 12건 무손실 + 동점 1건만 성공)
- [x] TV·학생 화면에 현재가·입찰모둠·타이머·로그 실시간 반영

### 4d. 결과 + 자산 순위  ✅ 통과 (2026-06-27, 실제 RTDB)
검증: `node scripts/verify-results.mjs`
- [x] 자산 = 잔여현금 + 낙찰작 감정가합, 모둠 순위 산출
- [x] 감정가·출처는 결과 단계(ResultsView)에서만 공개
- [x] 모둠별 "지불가 vs 감정가" 비교 시각화(이득/손해 색상)

### 4e. 데이터 내보내기  ✅ 통과 (2026-06-27)
- [x] 학생별 감상(작품·발문·답변) CSV, 번호 정렬 (BOM 포함 한글)
- [x] 모둠별 경매결과(낙찰작·지불가·잔액·감정가합·자산·순위) CSV, 순위 정렬
- [x] PDF: 브라우저 인쇄(window.print)로 PDF 저장 제공

### 4f. 프롤로그  ✅ 통과 (2026-06-27)
- [x] 학년군 3단계 서사, 단계별 "건너뛰기" + 진행 도트
- [x] 교사 편집 가능(ContentEditor → content에 저장, 프롤로그+발문 추가/삭제)

---

---

## v0.2.0 운영 기능 추가 (2026-06-30)  ✅ 완료

### 교사 입장 개편
- [x] 세션 생성 시 반 이름(필수)·교사 이름(선택) 입력 폼
- [x] 생성/입장 후 `SessionMeta`에 `className`, `teacherName` 저장
- [x] 이 기기에서 들어갔던 반 목록: 반 이름·교사 이름·코드 표시, 클릭 재입장
- [x] 코드 직접 입장 시 DB에서 반 정보 읽어 목록 자동 추가
- [x] 교사 콘솔 헤더에 반 이름·교사 이름 표시
- [x] `/teacher?code=XXX` 딥링크로 세션 바로 진입

### 슈퍼어드민
- [x] `/admin` 라우트, 별도 암호(write-once) 게이트
- [x] 전체 세션 실시간 목록: 반 이름·단계·접속 학생·작품·감상 답안 수
- [x] 코드/반 이름 검색, 교사 콘솔 열기·TV·QR 버튼
- [x] RTDB `adminAuth` 보안 규칙 추가
- [x] 홈 화면 슈퍼어드민 진입 링크

### 작품 비공개·가져오기 개편
- [x] 작품 카드 `🔒 공유 안 함` 체크박스 (`isPrivate` 필드)
- [x] `copyArtworksFrom` → `copySelectedArtworks` (배치 덮어쓰기 지원)
- [x] 가져오기: 반 선택 없이 전체 공유 작품 탐색 (공통감상/선택감상/경매대상 탭)
- [x] 가져올 위치 지정 (공통감상실 / 선택감상실) 라디오 선택
- [x] 비공개 작품은 탭 목록·카운트에서 제외

---

## 공통 비기능 체크(전 단계 누적)
- [x] 재접속 상태 복구(모든 화면이 RTDB 구독, 새로고침 시 서버 상태로 복원)
- [x] 저학년 UX: 큰 버튼·적은 글자·현재 단계 시각 표시
- [x] 다크 배경 명도 대비(골드/크림 토큰)
- [~] 태블릿 가로/세로: 기본 반응형. 실기기 회전 테스트는 배포 후 권장

## 알려진 단순화(프로토타입)
- 모둠 배정: 프로토타입은 입장 시 **학생 선택**(PRD 옵션). 교사 사전 매핑 UI는 후속.
- 모둠 대표: 학생이 직접 "대표하기"로 지정(먼저 누른 사람).
- PDF 내보내기: 전용 라이브러리 대신 브라우저 인쇄 사용.
- 감정가 숨김: 클라이언트 비표시 방식(보안규칙 분리는 후속, PRD §10 확정대로).

---

## 클라우드 연결
- [x] Firebase 실제 프로젝트 config 주입(.env) — `art-auction-8552` (Spark 무료)
- [x] Cloudflare Pages 배포, 세 라우트 동작 확인 — **https://art-auction.pages.dev** (무료)
- [ ] Cloudflare Worker + 이미지 업로드 실연결(선택, R2 카드 필요) — 현재는 이미지 URL 붙여넣기로 대체
